import {GetterTree} from 'vuex';
import {RootState} from '../../../src/store/types';
import ToolState from '../../../src/store/tools/types';

export const getters: GetterTree<ToolState, RootState> = {
    // asdf
};
