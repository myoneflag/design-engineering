import FlowSolver from '@/calculations/flow-solver';

describe('flow-solver.ts', () => {
    it('calculates flow for an example network', () => {
        // const solver = new FlowSolver(network, objectStore, doc, catalog);
    });
});
